## 📦 Layered Architecture 정리

### 1. Application Layer (애플리케이션)
- 비즈니스 로직의 흐름을 정의하고, `Domain`과 `Infrastructure`를 연결하는 계층
- 직접적인 데이터 처리보다는 **도메인에 위임**
- 가능한 한 **상태를 최소한으로 유지 (Stateless)**

---

### 2. Presentation Layer (표현 계층 / Controller)
- 사용자 요청(Request)을 해석하고 응답(Response)을 반환하는 계층
- API 엔드포인트 정의 및 클라이언트와의 통신 담당
- UI 제공 또는 HTTP 응답 처리 역할 수행

---

### 3. Domain Layer (도메인 / Model)
- **핵심 비즈니스 로직을 담당하는 계층**
- Entity를 중심으로 도메인 규칙과 상태 변화 관리
- 외부 계층에 대한 의존성을 최소화해야 함
- 외부 변화로부터 도메인 로직을 보호해야 함

---

### 4. Infrastructure Layer (인프라 / Repository)
- 외부 시스템(DB, API, OAuth 등)과의 통신 담당
- 환경 변화(외부 API, DB 등)에 가장 취약한 계층
- 도메인이 직접 의존하지 않도록 설계 필요

---

## 📁 Additional Packages

### Global
- 전역에서 공통으로 사용하는 요소들
- 예: `DTO`, `Error`, `Config`, `BaseTimeEntity` 등

---

### Exception
- 기능별 커스텀 예외 클래스 관리
- 예외 처리 정책을 일관되게 유지

---

### DTO (Data Transfer Object)
- 계층 간 데이터 전달을 위한 객체
- `Application` / `Presentation` 계층에서 사용
- **비즈니스 로직 포함 금지**
- 역할:
    - 데이터 전달
    - 직렬화 / 역직렬화
    - 요청 / 응답 모델 정의